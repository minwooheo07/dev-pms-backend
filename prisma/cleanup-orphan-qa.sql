-- ─────────────────────────────────────────────────────────────
-- 일회성 정리 스크립트: 일감(WorkLog)이 이미 삭제되어 링크가 끊긴 기존 QA 정리
-- (workLogDeleted 플래그 도입 이전에 삭제된 일감의 QA는 workLogId가 NULL로만 남아있음)
--
-- ⚠️ 주의: "QA 등록" 버튼으로 수동 생성한 QA도 workLogId가 NULL입니다.
--         데이터 클렌징 상황이면 보통 모두 정리 대상이지만, 보존할 QA가 있다면
--         먼저 SELECT로 확인 후 실행하세요.
-- ─────────────────────────────────────────────────────────────

-- 0) 정리 대상 미리 확인
SELECT id, "qaNumber", "srNumber", title, status, "createdAt"
FROM "QATest"
WHERE "workLogId" IS NULL
ORDER BY "createdAt";

-- ─────────────────────────────────────────────────────────────
-- [옵션 A] 화면에 "삭제된 일감"으로 표기 + 삭제 버튼 활성화만 하고 데이터는 보존
--          (UI에서 하나씩 확인하며 삭제하고 싶을 때)
-- ─────────────────────────────────────────────────────────────
-- UPDATE "QATest" SET "workLogDeleted" = true WHERE "workLogId" IS NULL;

-- ─────────────────────────────────────────────────────────────
-- [옵션 B] 끊긴 QA를 즉시 일괄 삭제 (완전 정리 — 권장: 데이터 클렌징 중)
-- ─────────────────────────────────────────────────────────────
-- DELETE FROM "QATest" WHERE "workLogId" IS NULL;
